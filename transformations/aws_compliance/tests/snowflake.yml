kind: destination
spec:
  name: dest
  path: cloudquery/snowflake
  version: "v3.2.11" # latest version of destination snowflake plugin
  spec:
    connection_string: ${SNOWFLAKE_CONNECTION_STRING}
    migrate_concurrency: -1
