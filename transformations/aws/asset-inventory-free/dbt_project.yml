name: aws_asset_inventory
version: 2.2.7
config-version: 2
profile: aws_asset_inventory
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
  - ../../macros
snapshot-paths:
  - snapshots
clean-targets:
  - target
  - dbt_packages
models: null
dispatch:
  - macro_namespace: dbt_utils
    search_order:
      - aws_asset_inventory
      - dbt_utils
