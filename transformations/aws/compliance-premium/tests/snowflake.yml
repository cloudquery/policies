kind: source
spec:
  name: aws
  path: cloudquery/aws
  registry: cloudquery
  version: "v23.3.1" # latest version of source aws plugin
  destinations: ["snowflake"]
  tables: ["*"]
---
kind: destination
spec:
  name: snowflake
  path: cloudquery/snowflake
  registry: github
  version: "v3.3.6" # latest version of destination snowflake plugin
  spec:
    connection_string: ${SNOWFLAKE_CONNECTION_STRING}
    migrate_concurrency: 50
