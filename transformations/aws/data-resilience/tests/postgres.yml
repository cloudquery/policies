kind: source
spec:
  name: aws
  path: cloudquery/aws
  registry: cloudquery
  version: "v24.3.1" # latest version of source aws plugin
  destinations: ["postgresql"]
  tables: ["*"]
---
kind: destination
spec:
  name: "postgresql"
  path: "cloudquery/postgresql"
  registry: cloudquery
  version: "v7.3.4" # latest version of postgresql plugin
  spec:
    batch_size: 10000
    connection_string: ${CQ_DSN}
