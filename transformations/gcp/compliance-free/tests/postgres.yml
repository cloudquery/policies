kind: source
spec:
  name: gcp
  path: cloudquery/gcp
  version: "v12.0.1" # latest version of source gcp plugin
  destinations: ["postgresql"]
  tables: ["*"]
---
kind: destination
spec:
  name: "postgresql"
  path: "cloudquery/postgresql"
  version: "v7.4.0" # latest version of postgresql plugin
  spec:
    batch_size: 10000
    connection_string: ${CQ_DSN}