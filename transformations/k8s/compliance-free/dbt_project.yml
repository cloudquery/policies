name: k8s_compliance
version: 1.3.0
config-version: 2
profile: k8s_compliance
model-paths:
  - models
  - ../models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
  - ../macros
snapshot-paths:
  - snapshots
clean-targets:
  - target
  - dbt_packages
models:
  k8s_compliance: null
